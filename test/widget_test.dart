import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PhishGuard AI smoke test', (WidgetTester tester) async {
    // Placeholder smoke test - full tests require Hive + provider setup
    expect(1 + 1, 2);
  });
}
